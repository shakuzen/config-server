spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/shakuzen/config-repo-5491.git
          search-paths: '*'
        bootstrap: true
      fail-fast: true
